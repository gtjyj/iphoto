{
  "name": "i-photo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:web": "pnpm -r --filter=./packages/web run start",
    "build:web": "pnpm -r --filter=./packages/web run build",
    "dev:server": "pnpm -r --filter=./packages/server run start",
    "build:server": "pnpm -r --filter=./packages/server run build",
    "start:server": "pnpm build:server && pm2 start ./packages/server/dist/main.js --name iphoto",
    "stop:server": "pm2 stop iphoto",
    "start:server-1panel": "pnpm build:server && cd ./packages/server/ && node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/preset-env": "^7.26.9",
    "pm2": "^6.0.5"
  }
}
